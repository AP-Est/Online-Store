import { storeData } from '../data/data';

export class MainPageModel {
    products: object;
    //productsChanged: object;

    constructor() {
        this.products = storeData;
        //this.productsChanged =
    }

    /*filterProducts(category?: string, brand?: string, minPrice?: number, maxPrice?: number, minStock?: number, maxStock?: number){
        this.productsChanged = this.products.filter();

        this.productsChanged(this.products);
        this.categoryChanged(this.products, this.productsChanged);
        this.brandChanged(this.products, this.productsChanged);
        this.priceChanged(this.products, this.productsChanged);
        this.stockChanged(this.products, this.productsChanged);
    } */
}
